-- =============================================================================
-- SlangBase 数据库结构 (SQLite)
-- =============================================================================

-- -----------------------------------------------------------------------------
-- 标签类型 (tag_types)
-- -----------------------------------------------------------------------------
-- 用来给标签分组/分类，例如 "领域"、"部门"、"业务线" 等。
-- -----------------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS tag_types (
    id         TEXT PRIMARY KEY,
    name       TEXT NOT NULL UNIQUE, --   name  : 用户可见名（唯一）
    color      TEXT NOT NULL,        --   color : #RRGGBB
    created_at TEXT NOT NULL
);
